/*
 * File intended to be used to simulate traversal over the network. Since we
 * will not actually have access to a large system, we will need to simulate
 * features such as network delays and message drops in order to properly
 * reflect network behavior.
 */
